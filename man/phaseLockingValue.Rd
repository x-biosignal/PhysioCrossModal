% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-phase.R
\name{phaseLockingValue}
\alias{phaseLockingValue}
\title{Phase Locking Value (PLV)}
\usage{
phaseLockingValue(
  x,
  y = NULL,
  sr = NULL,
  freq_band,
  method = c("hilbert", "wavelet"),
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L
)
}
\arguments{
\item{x}{Numeric vector, PhysioExperiment, or MultiPhysioExperiment.}

\item{y}{Numeric vector or PhysioExperiment (NULL when \code{x} is an MPE).}

\item{sr}{Numeric sampling rate in Hz (required when x/y are numeric).}

\item{freq_band}{Numeric vector of length 2 specifying the frequency band
\code{c(low, high)} in Hz. Required.}

\item{method}{Character; phase extraction method. Currently only
\code{"hilbert"} is supported. \code{"wavelet"} is reserved for future use.}

\item{modality_x}{Character modality name in MPE for x signal.}

\item{modality_y}{Character modality name in MPE for y signal.}

\item{channels_x}{Integer channel index to extract from x (default 1).}

\item{channels_y}{Integer channel index to extract from y (default 1).}
}
\value{
A named list with components:
\describe{
\item{plv}{Numeric scalar in [0, 1] -- the Phase Locking Value.}
\item{phase_diff}{Numeric vector of instantaneous phase differences
(radians, [-pi, pi]).}
}
}
\description{
Computes the Phase Locking Value between two signals, measuring the
consistency of phase difference across time. A PLV of 1 indicates
perfect phase locking; a PLV of 0 indicates no consistent phase
relationship.
}
\details{
The signals are bandpass-filtered to \code{freq_band} and then Hilbert-
transformed to extract instantaneous phase. PLV is computed as:
\deqn{\text{PLV} = \left|\frac{1}{N} \sum_{t=1}^{N} e^{i(\phi_x(t) - \phi_y(t))}\right|}
}
\examples{
# Two phase-locked sinusoids
sr <- 500
t <- seq(0, 2, length.out = sr * 2)
x <- sin(2 * pi * 10 * t)
y <- sin(2 * pi * 10 * t + pi / 4)
result <- phaseLockingValue(x, y, sr = sr, freq_band = c(8, 12))
result$plv
}
\references{
Lachaux, J.-P., Rodriguez, E., Martinerie, J., & Varela, F. J. (1999).
Measuring phase synchrony in brain signals. \emph{Human Brain Mapping},
8(4), 194--208.
}
\seealso{
\code{\link[=phaseLagIndex]{phaseLagIndex()}}, \code{\link[=weightedPLI]{weightedPLI()}}, \code{\link[=waveletPLV]{waveletPLV()}},
\code{\link[=couplingAnalysis]{couplingAnalysis()}}
}
