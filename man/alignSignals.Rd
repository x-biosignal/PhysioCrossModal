% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align-signals.R
\name{alignSignals}
\alias{alignSignals}
\title{Align multiple PhysioExperiment objects to a common sampling rate}
\usage{
alignSignals(
  ...,
  method = c("lowest_rate", "common_rate", "resample", "elastic"),
  target_rate = NULL
)
}
\arguments{
\item{...}{Named \code{PhysioExperiment} objects.}

\item{method}{Character: one of \code{"lowest_rate"} (default),
\code{"common_rate"}, \code{"resample"}, or \code{"elastic"}.}

\item{target_rate}{Numeric scalar: required when \code{method = "resample"}.}
}
\value{
A \code{\link{MultiPhysioExperiment}} containing the (possibly
resampled) input objects.
}
\description{
Takes two or more named \code{PhysioExperiment} objects and resamples them
so they all share a single sampling rate, then wraps the result in a
\code{\link{MultiPhysioExperiment}}.
}
\details{
Three alignment strategies are available:
\describe{
\item{\code{"lowest_rate"}}{(default) Resample all signals to the lowest
sampling rate present.}
\item{\code{"common_rate"}}{Resample all signals to the highest sampling
rate present.}
\item{\code{"resample"}}{Resample all signals to the rate given by
\code{target_rate} (which must be supplied).}
\item{\code{"elastic"}}{Resample to the lowest rate, then additionally
apply an elastic (SRVF) time warping of every object onto the first, so
that shared events line up in phase. The warping is computed from each
object's first channel and carried to all its channels; it is stored in
\code{metadata()$elastic_warp}. Objects whose duration differs from the
reference after rate alignment are left un-warped. See \code{\link[=elasticAlign]{elasticAlign()}}.}
}

If every input already shares the same sampling rate, no resampling is
performed regardless of the chosen method (elastic warping, if requested, is
still applied).
}
\examples{
eeg <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(500 * 2), nrow = 500)),
  samplingRate = 500
)
emg <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000 * 2), nrow = 1000)),
  samplingRate = 1000
)
mpe <- alignSignals(EEG = eeg, EMG = emg, method = "lowest_rate")
}
\seealso{
\code{\link[=alignToRate]{alignToRate()}}, \code{\link[=mergePhysio]{mergePhysio()}},
\code{\link[=MultiPhysioExperiment]{MultiPhysioExperiment()}}, \code{\link[=couplingAnalysis]{couplingAnalysis()}}
}
