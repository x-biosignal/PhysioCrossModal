% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-multiblock.R
\name{jive}
\alias{jive}
\title{JIVE: Joint and Individual Variation Explained}
\usage{
jive(
  blocks,
  rank_joint = 1L,
  rank_individual = 1L,
  center = TRUE,
  scale = TRUE,
  max_iter = 1000L,
  tol = 1e-08
)
}
\arguments{
\item{blocks}{A list of >= 2 matrices, each \verb{n x p_k}: the same \code{n}
observations (e.g. subjects, or gait-cycle points), different features per
modality.}

\item{rank_joint}{Rank of the joint structure.}

\item{rank_individual}{Rank of each block's individual structure (a scalar,
recycled, or a per-block vector).}

\item{center, scale}{Row-center each feature and scale each block to unit
Frobenius norm (so blocks contribute comparably); both default \code{TRUE}.}

\item{max_iter, tol}{Iteration budget and convergence tolerance.}
}
\value{
a \code{jive} object: \code{joint} and \code{individual} (lists of \verb{n x p_k}
matrices), \code{joint_scores} (\verb{n x rank_joint}, the shared component scores),
\code{variance} (data frame of joint / individual / residual fraction per block),
\code{ranks}, \code{iterations}.
}
\description{
Decomposes several modality blocks that share an observation dimension into a
low-rank JOINT structure common to all blocks, low-rank INDIVIDUAL structure
specific to each, and residual noise (Lock et al. 2013). The joint scores are
the integrated multi-modal representation; the per-block variance split tells
you how much of each modality is shared versus idiosyncratic.
}
\examples{
set.seed(1)
shared <- matrix(rnorm(40 * 2), 40, 2)                 # common subject scores
k <- shared \%*\% matrix(rnorm(2 * 6), 2, 6) + matrix(rnorm(40 * 6, 0, .3), 40, 6)
e <- shared \%*\% matrix(rnorm(2 * 5), 2, 5) + matrix(rnorm(40 * 5, 0, .3), 40, 5)
fit <- jive(list(kin = k, emg = e), rank_joint = 2, rank_individual = 1)
fit$variance
}
\references{
Lock EF, Hoadley KA, Marron JS, Nobel AB (2013) Ann Appl Stat
7:523-542.
}
\seealso{
\code{\link[=multipleFactorAnalysis]{multipleFactorAnalysis()}}, \code{\link[=cca]{cca()}}
}
