% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-association.R
\name{rvCoefficient}
\alias{rvCoefficient}
\title{RV coefficient between two modality blocks}
\usage{
rvCoefficient(X, Y, center = TRUE, permutations = 0L, seed = NULL)
}
\arguments{
\item{X, Y}{Matrices with the same \code{n} rows (observations).}

\item{center}{Column-centre each block first (default \code{TRUE}).}

\item{permutations}{Row permutations for the null p-value (default 0 = skip).}

\item{seed}{Optional RNG seed for the permutation test.}
}
\value{
an \code{rv_coefficient} object: \code{rv}, \code{p_value}, \code{n}.
}
\description{
The multivariate generalisation of a squared correlation: how much the two
blocks' configurations of observations agree, in [0, 1] (0 = unrelated, 1 =
identical up to rotation/scaling).
}
\examples{
set.seed(1); z <- matrix(rnorm(40 * 2), 40, 2)
X <- z \%*\% matrix(rnorm(2 * 5), 2, 5) + matrix(rnorm(40 * 5, 0, .3), 40, 5)
Y <- z \%*\% matrix(rnorm(2 * 4), 2, 4) + matrix(rnorm(40 * 4, 0, .3), 40, 4)
rvCoefficient(X, Y)$rv
}
\references{
Robert P, Escoufier Y (1976) Appl Stat 25:257-265.
}
\seealso{
\code{\link[=distanceCorrelation]{distanceCorrelation()}}, \code{\link[=representationalSimilarity]{representationalSimilarity()}}, \code{\link[=cca]{cca()}}
}
