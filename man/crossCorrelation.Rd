% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-timedom.R
\name{crossCorrelation}
\alias{crossCorrelation}
\title{Cross-correlation between two signals}
\usage{
crossCorrelation(
  x,
  y = NULL,
  sr = NULL,
  max_lag = NULL,
  normalize = TRUE,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L
)
}
\arguments{
\item{x}{Numeric vector, PhysioExperiment, or MultiPhysioExperiment.}

\item{y}{Numeric vector or PhysioExperiment, or NULL when \code{x} is
a MultiPhysioExperiment.}

\item{sr}{Numeric sampling rate in Hz (required when x/y are numeric).}

\item{max_lag}{Integer maximum lag in samples. If NULL, defaults to
\code{floor(length(x) / 4)}.}

\item{normalize}{Logical; if TRUE (default), compute normalized
cross-correlation (Pearson-like, values in [-1, 1]).}

\item{modality_x}{Character modality name in MPE for the x signal.}

\item{modality_y}{Character modality name in MPE for the y signal.}

\item{channels_x}{Integer which channel to extract from x (default 1).}

\item{channels_y}{Integer which channel to extract from y (default 1).}
}
\value{
A named list with components:
\describe{
\item{correlation}{Numeric vector of cross-correlation values at each
lag.}
\item{lags}{Integer vector of lag values in samples.}
\item{lag_seconds}{Numeric vector of lag values in seconds.}
\item{peak_lag}{Integer lag (in samples) at which the absolute
correlation is maximised.}
\item{peak_lag_seconds}{Numeric peak lag converted to seconds.}
\item{peak_correlation}{Numeric cross-correlation value at the peak
lag.}
}
}
\description{
Computes the cross-correlation between two signals at various lags,
measuring their time-domain coupling. Cross-correlation quantifies how
similar one signal is to a time-shifted version of another. A positive
peak lag indicates that \code{y} leads \code{x} (i.e., \code{y} must be shifted forward
to align with \code{x}), while a negative peak lag indicates \code{x} leads \code{y}.
}
\details{
The function accepts numeric vectors, PhysioExperiment objects, or a
MultiPhysioExperiment with named modalities, using
\code{.extract_signal_pair()} internally for flexible input handling.
}
\examples{
# Simple example: y is a delayed copy of x
sr <- 500
set.seed(1)
x <- rnorm(1000)
y <- c(rep(0, 10), x[1:990])
result <- crossCorrelation(x, y, sr = sr)
result$peak_lag       # should be 10
result$peak_lag_seconds
}
\references{
Chatfield, C. (2004). \emph{The Analysis of Time Series: An
Introduction} (6th ed.). Chapman & Hall/CRC.
}
\seealso{
\code{\link[=slidingCrossCorrelation]{slidingCrossCorrelation()}}, \code{\link[=coherence]{coherence()}},
\code{\link[=couplingAnalysis]{couplingAnalysis()}}
}
