% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-frontier.R
\name{phaseSlopeIndex}
\alias{phaseSlopeIndex}
\title{Phase Slope Index (directed, volume-conduction robust)}
\usage{
phaseSlopeIndex(
  x,
  y = NULL,
  sr = NULL,
  freq_band,
  nperseg = 256L,
  noverlap = NULL,
  normalize = TRUE,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L
)
}
\arguments{
\item{x, y}{Signals (or a single object from which a pair is extracted).}

\item{sr}{Sampling rate in Hz.}

\item{freq_band}{Numeric \code{c(low, high)} band in Hz.}

\item{nperseg, noverlap}{Welch segment length / overlap for the cross-spectrum.}

\item{normalize}{If \code{TRUE} (default), scale into \verb{[-1, 1]} by the summed
coherency magnitude; if \code{FALSE}, return the raw imaginary sum.}

\item{modality_x, modality_y, channels_x, channels_y}{Passed to the internal
signal-pair extractor.}
}
\value{
A single Phase Slope Index value.
}
\description{
The Phase Slope Index (Nolte et al. 2008) estimates the direction of
information flow from the slope of the coherency phase across a frequency
band, and is insensitive to instantaneous (zero-lag) mixing. Positive values
indicate that \code{x} leads (drives) \code{y}.
}
\examples{
set.seed(1); sr <- 200; n <- 4000
x <- as.numeric(stats::filter(rnorm(n), rep(1/5, 5), sides = 2)); x[is.na(x)] <- 0
y <- c(rep(0, 8), x[seq_len(n - 8)])          # y lags x
phaseSlopeIndex(x, y, sr = sr, freq_band = c(1, 40))
}
\references{
Nolte G, et al. (2008). Robustly estimating the flow direction of
information in complex physical systems. \emph{Phys Rev Lett} 100:234101.
\doi{10.1103/PhysRevLett.100.234101}
}
