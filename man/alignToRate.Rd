% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align-signals.R
\name{alignToRate}
\alias{alignToRate}
\title{Resample a PhysioExperiment to a target sampling rate}
\usage{
alignToRate(x, target_rate, method = c("linear", "spline", "fft"))
}
\arguments{
\item{x}{A \code{PhysioExperiment} object.}

\item{target_rate}{Numeric scalar: desired sampling rate in Hz.}

\item{method}{Character: resampling method. One of \code{"linear"}
(default), \code{"spline"}, or \code{"fft"}.}
}
\value{
A new \code{PhysioExperiment} with resampled data and updated
\code{samplingRate}. The assay name is preserved from the original.
}
\description{
Resamples all assays in a \code{PhysioExperiment} object so that the
resulting data correspond to the specified target sampling rate. If the
\pkg{PhysioPreprocess} package is available and \code{method = "linear"},
its \code{resample()} function is used; otherwise an internal
interpolation is applied.
}
\examples{
eeg_data <- matrix(rnorm(1000 * 4), nrow = 1000, ncol = 4)
pe <- PhysioExperiment(
  assays = list(raw = eeg_data),
  colData = S4Vectors::DataFrame(
    label = paste0("Ch", 1:4),
    type = rep("EEG", 4)
  ),
  samplingRate = 1000
)
pe500 <- alignToRate(pe, target_rate = 500)
samplingRate(pe500)  # 500
}
\seealso{
\code{\link[=alignSignals]{alignSignals()}}, \code{\link[=mergePhysio]{mergePhysio()}},
\code{\link[=MultiPhysioExperiment]{MultiPhysioExperiment()}}
}
