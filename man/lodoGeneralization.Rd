% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark-lodo.R
\name{lodoGeneralization}
\alias{lodoGeneralization}
\title{Leave-one-site-out generalization benchmark}
\usage{
lodoGeneralization(
  data,
  outcome,
  site,
  features = NULL,
  family = c("gaussian", "binomial"),
  positive_class = NULL,
  threshold = 0.5,
  min_train_rows = 20L,
  scale_features = TRUE
)
}
\arguments{
\item{data}{Data frame containing outcome, site label, and features.}

\item{outcome}{Outcome column name.}

\item{site}{Site/facility column name used for LODO splitting.}

\item{features}{Feature column names. If \code{NULL}, numeric columns excluding
\code{outcome} and \code{site} are used.}

\item{family}{Modeling family: \code{"gaussian"} or \code{"binomial"}.}

\item{positive_class}{Positive class label for binomial metrics.
If \code{NULL}, the second factor level is used.}

\item{threshold}{Classification threshold for binomial predictions.}

\item{min_train_rows}{Minimum training rows required per fold.}

\item{scale_features}{Logical; z-score features using training statistics.}
}
\value{
A list with:
\describe{
\item{fold_metrics}{Per-site metrics.}
\item{predictions}{Row-level held-out predictions.}
\item{aggregate}{Mean metrics across folds.}
\item{settings}{Benchmark settings and feature list.}
}
}
\description{
Evaluates model transportability by training on all-but-one sites and
testing on the held-out site (LODO). Supports continuous outcomes
(\code{family = "gaussian"}) and binary outcomes (\code{family = "binomial"}).
}
\examples{
set.seed(1)
n <- 120
df <- data.frame(
  site_id = rep(c("A", "B", "C"), each = 40),
  f1 = rnorm(n),
  f2 = rnorm(n)
)
df$y <- 0.8 * df$f1 - 0.4 * df$f2 + rnorm(n, sd = 0.2)

res <- lodoGeneralization(
  data = df,
  outcome = "y",
  site = "site_id",
  family = "gaussian"
)
head(res$fold_metrics)
}
\seealso{
\code{\link[=couplingAnalysis]{couplingAnalysis()}}, \code{\link[=surrogateTest]{surrogateTest()}}
}
