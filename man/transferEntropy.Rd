% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-directed.R
\name{transferEntropy}
\alias{transferEntropy}
\title{Transfer entropy (model-free directed information)}
\usage{
transferEntropy(
  x,
  y = NULL,
  sr = NULL,
  k = 1L,
  l = 1L,
  delay = 1L,
  estimator = c("ksg", "histogram"),
  knn = 4L,
  bins = 8L,
  effective = FALSE,
  n_surrogate = 19L,
  seed = NULL,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L
)
}
\arguments{
\item{x, y}{Two numeric signals or PhysioExperiment objects (or a
MultiPhysioExperiment via \code{x}).}

\item{sr}{Sampling rate in Hz (required for numeric input).}

\item{k}{Target (destination) embedding length (default: 1).}

\item{l}{Source embedding length (default: 1).}

\item{delay}{Source-to-target lag in samples (default: 1).}

\item{estimator}{\code{"ksg"} (k-NN, in nats) or \code{"histogram"}
(binning, in bits).}

\item{knn}{Number of neighbours for the KSG estimator (default: 4).}

\item{bins}{Number of bins per dimension for the histogram estimator
(default: 8).}

\item{effective}{Subtract the surrogate (IAAFT) null mean (default:
\code{FALSE}).}

\item{n_surrogate}{Number of surrogates for \code{effective} (default: 19).}

\item{seed}{Optional RNG seed for reproducible surrogates.}

\item{modality_x, modality_y, channels_x, channels_y}{Passed to the shared
signal-pair extraction.}
}
\value{
A list with \code{te_xy}, \code{te_yx}, and \code{net}
(\code{te_xy - te_yx}); when \code{effective = TRUE} also \code{eff_xy},
\code{eff_yx}, and the surrogate null standard deviations.
}
\description{
Estimates the transfer entropy from \code{x} to \code{y} and back
(Schreiber 2000), a model-free measure of directed information flow, with a
histogram (binning) estimator or the Kraskov-Stoegbauer-Grassberger (KSG)
k-nearest-neighbour estimator. With \code{effective = TRUE} the estimate is
bias-corrected by subtracting the mean transfer entropy of IAAFT surrogates
of the source, so independent signals give an effective transfer entropy near
zero.
}
\examples{
set.seed(1)
x <- rnorm(600); y <- c(rep(0, 5), 0.7 * x[1:595]) + rnorm(600, sd = 0.5)
transferEntropy(x, y, sr = 100, delay = 5, estimator = "histogram")$net
}
\references{
Schreiber, T. (2000). Measuring information transfer. Physical Review
Letters, 85(2), 461-464.

Kraskov, A., Stoegbauer, H., & Grassberger, P. (2004). Estimating mutual
information. Physical Review E, 69(6), 066138.
}
\seealso{
\code{\link[=directedTransferFunction]{directedTransferFunction()}}, \code{\link[=grangerCausality]{grangerCausality()}},
\code{\link[=surrogateTest]{surrogateTest()}}
}
