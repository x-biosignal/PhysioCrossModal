% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-twoblock.R
\name{plsBlocks}
\alias{plsBlocks}
\title{Two-block partial least squares (PLS-SVD)}
\usage{
plsBlocks(X, Y, ncomp = 2L)
}
\arguments{
\item{X, Y}{Matrices with the same \code{n} rows (observations).}

\item{ncomp}{Number of PLS components (default 2).}
}
\value{
a \code{pls_blocks} object: \code{xweights}, \code{yweights} (the paired directions),
\code{xscores}, \code{yscores}, \code{covariance} (per-component score covariance) and
\code{correlation} (per-component score correlation).
}
\description{
Finds the linear combinations of two modality blocks with maximal cross-modal
COVARIANCE (unlike CCA's correlation), by the singular value decomposition of
the cross-covariance and successive deflation. Robust when features outnumber
observations, so it suits high-dimensional modalities.
}
\examples{
set.seed(1); z <- matrix(rnorm(40), 40, 1)
X <- z \%*\% matrix(rnorm(6), 1, 6) + matrix(rnorm(40 * 6, 0, .3), 40, 6)
Y <- z \%*\% matrix(rnorm(5), 1, 5) + matrix(rnorm(40 * 5, 0, .3), 40, 5)
plsBlocks(X, Y, ncomp = 1)$correlation
}
\references{
Wold H (1975); Bookstein (1994) PLS-SVD; Krishnan et al. (2011).
}
\seealso{
\code{\link[=cca]{cca()}}, \code{\link[=coupledNMF]{coupledNMF()}}
}
