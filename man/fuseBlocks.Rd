% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-index.R
\name{fuseBlocks}
\alias{fuseBlocks}
\title{Feature-level fusion of modality blocks}
\usage{
fuseBlocks(blocks, method = c("zscore", "mfa", "frobenius"))
}
\arguments{
\item{blocks}{A list of matrices, each \verb{n x p_k} (shared rows).}

\item{method}{Block scaling before concatenation: \code{"zscore"} (per-column
standardise, default), \code{"mfa"} (divide each block by its leading singular
value), or \code{"frobenius"} (unit Frobenius norm per block).}
}
\value{
a \code{fused_blocks} object: \code{matrix} (\verb{n x sum(p_k)}) and \code{block_index}
(column ranges per block).
}
\description{
Concatenates several modality blocks into a single design matrix for
downstream (multi-view) machine learning, scaling each block so a large or
high-variance modality does not dominate the fused features.
}
\examples{
fuseBlocks(list(a = matrix(rnorm(20), 10, 2), b = matrix(rnorm(30) * 50, 10, 3)))$matrix
}
\seealso{
\code{\link[=multipleFactorAnalysis]{multipleFactorAnalysis()}}, \code{\link[=multimodalGaitIndex]{multimodalGaitIndex()}}
}
