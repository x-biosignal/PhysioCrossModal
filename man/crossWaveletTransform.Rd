% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-xwt.R
\name{crossWaveletTransform}
\alias{crossWaveletTransform}
\title{Cross-wavelet transform (XWT) with red-noise significance}
\usage{
crossWaveletTransform(
  x,
  y = NULL,
  sr = NULL,
  frequencies = seq(1, 40, by = 1),
  n_cycles = 7,
  significance = TRUE,
  n_surrogates = 100L,
  siglvl = 0.95,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L
)
}
\arguments{
\item{x, y}{Signals (or a single object from which a pair is extracted).}

\item{sr}{Sampling rate in Hz.}

\item{frequencies}{Frequencies (Hz) to analyse (default \code{seq(1, 40, 1)}).}

\item{n_cycles}{Morlet wavelet cycles (default 7).}

\item{significance}{If \code{TRUE} (default), compute red-noise significance.}

\item{n_surrogates}{Number of AR(1) surrogate pairs (default 100).}

\item{siglvl}{Significance quantile (default 0.95).}

\item{modality_x, modality_y, channels_x, channels_y}{Passed to the pair extractor.}
}
\value{
An object of class \code{"cross_wavelet"}: a list with \code{power}
(\verb{time x frequency} cross-wavelet modulus), \code{phase} (relative phase, radians;
positive = \code{x} leads \code{y}), \code{cross} (complex XWT), \code{frequencies}, \code{times},
\code{coi} (cone of influence), and — when \code{significance} — \code{sig_level} (per
frequency), \code{sig_ratio} (power / level), and \code{significant} (logical mask).
}
\description{
Computes the cross-wavelet transform \eqn{W_{xy} = W_x W_y^*} of two signals
via the Morlet wavelet: its modulus is the common time-frequency power and its
argument is the phase lead/lag between the signals. Common power is tested
against an AR(1) red-noise background (Torrence & Compo 1998; Grinsted et al.
2004) by Monte-Carlo: AR(1) surrogates matched to each signal's lag-1
autocorrelation and variance are transformed identically, and the per-
frequency \code{siglvl} quantile of the surrogate cross-power sets the threshold.
}
\examples{
set.seed(1); sr <- 100; t <- seq(0, 20, by = 1 / sr)
x <- sin(2 * pi * 10 * t) + rnorm(length(t), sd = 0.5)
y <- sin(2 * pi * 10 * t - pi / 2) + rnorm(length(t), sd = 0.5)  # y lags x
xw <- crossWaveletTransform(x, y, sr = sr, frequencies = seq(4, 16, by = 1),
                            n_surrogates = 50)
mean(xw$significant)
}
\references{
Torrence C, Compo GP (1998). A practical guide to wavelet analysis.
\emph{Bull Amer Meteor Soc} 79:61-78.
Grinsted A, Moore JC, Jevrejeva S (2004). Application of the cross wavelet
transform and wavelet coherence to geophysical time series.
\emph{Nonlin Processes Geophys} 11:561-566.
}
