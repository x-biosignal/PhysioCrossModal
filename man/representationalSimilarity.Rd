% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-association.R
\name{representationalSimilarity}
\alias{representationalSimilarity}
\title{Representational similarity across modalities (RSA)}
\usage{
representationalSimilarity(
  blocks,
  method = c("euclidean", "correlation"),
  compare = c("spearman", "pearson"),
  permutations = 0L,
  seed = NULL
)
}
\arguments{
\item{blocks}{A list of >= 2 matrices, each \verb{n x p_k} (shared rows).}

\item{method}{RDM distance: \code{"euclidean"} (default) or \code{"correlation"}
(\code{1 - cor}).}

\item{compare}{RDM comparison: \code{"spearman"} (default) or \code{"pearson"}.}

\item{permutations}{For a PAIR of blocks, Mantel permutations for a p-value
(default 0).}

\item{seed}{Optional RNG seed.}
}
\value{
an \code{rsa} object: \code{similarity} (block x block RSA matrix) and, for two
blocks with \code{permutations > 0}, \code{p_value} (Mantel).
}
\description{
Compares the \emph{geometry of observations} between modalities: each block's
representational dissimilarity matrix (RDM -- pairwise distances between the
shared observations, using that modality's features) is compared across blocks.
Two modalities are "representationally similar" if observations that are far
apart in one are far apart in the other.
}
\examples{
set.seed(1); z <- matrix(rnorm(30 * 2), 30, 2)
a <- z \%*\% matrix(rnorm(2 * 5), 2, 5); b <- z \%*\% matrix(rnorm(2 * 4), 2, 4)
representationalSimilarity(list(a = a, b = b))$similarity
}
\references{
Kriegeskorte N, et al. (2008) Front Syst Neurosci 2:4.
}
\seealso{
\code{\link[=rvCoefficient]{rvCoefficient()}}, \code{\link[=distanceCorrelation]{distanceCorrelation()}}
}
