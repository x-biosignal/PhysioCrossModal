% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-matrix.R
\name{coherenceMatrix}
\alias{coherenceMatrix}
\title{Coherence matrix across channel pairs}
\usage{
coherenceMatrix(
  x,
  y = NULL,
  sr = NULL,
  channels_x = NULL,
  channels_y = NULL,
  modality_x = NULL,
  modality_y = NULL,
  ...
)
}
\arguments{
\item{x}{A PhysioExperiment or MultiPhysioExperiment object.}

\item{y}{A PhysioExperiment, or NULL when \code{x} is an MPE.}

\item{sr}{Numeric sampling rate in Hz (used only for numeric inputs).}

\item{channels_x}{Integer vector of channel indices for x, or NULL
for all channels.}

\item{channels_y}{Integer vector of channel indices for y, or NULL
for all channels.}

\item{modality_x, modality_y}{Character modality names for MPE input.}

\item{...}{Additional arguments passed to \code{\link{coherence}}
(e.g. \code{nperseg}, \code{freq_range}).}
}
\value{
A list with components:
\describe{
\item{matrix}{Numeric matrix of peak coherence values, with
dimensions \code{[n_channels_x x n_channels_y]}.}
\item{spectra}{List of lists containing the full coherence result
for each pair.}
\item{frequencies}{Numeric vector of frequencies from the first pair.}
\item{channel_names_x}{Character vector of x channel names.}
\item{channel_names_y}{Character vector of y channel names.}
}
}
\description{
Computes magnitude-squared coherence for all pairs of channels between
two sets of signals (or between all channels within a single set).
Returns a matrix of peak coherence values plus the full spectra.
}
\examples{
sr <- 200; n <- sr * 2
pe1 <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(n * 2), nrow = n)),
  samplingRate = sr
)
pe2 <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(n * 2), nrow = n)),
  samplingRate = sr
)
result <- coherenceMatrix(pe1, pe2, nperseg = 64L)
result$matrix
}
\references{
Carter, G. C. (1987). Coherence and time delay estimation.
\emph{Proceedings of the IEEE}, 75(2), 236--255.
}
\seealso{
\code{\link[=coherence]{coherence()}}, \code{\link[=couplingMatrix]{couplingMatrix()}}, \code{\link[=plotCouplingMatrix]{plotCouplingMatrix()}},
\code{\link[=surrogateMatrixTest]{surrogateMatrixTest()}}
}
