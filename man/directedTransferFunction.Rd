% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-directed.R
\name{directedTransferFunction}
\alias{directedTransferFunction}
\title{Directed Transfer Function for multichannel / multimodal signals}
\usage{
directedTransferFunction(
  x,
  y = NULL,
  sr = NULL,
  order = 5L,
  freqs = NULL,
  normalized = TRUE,
  ffDTF = FALSE,
  band = NULL,
  mvar_method = "ols",
  modality_x = NULL,
  modality_y = NULL,
  channels_x = NULL,
  channels_y = NULL
)
}
\arguments{
\item{x}{A multichannel matrix (time x channels) or PhysioExperiment (with
\code{y = NULL}), or one signal / PhysioExperiment of a pair.}

\item{y}{The second signal / PhysioExperiment of a pair, or \code{NULL} for a
single multichannel input.}

\item{sr}{Sampling rate in Hz (required for numeric-matrix input).}

\item{order}{MVAR model order (default: 5), or \code{NULL} to select
automatically.}

\item{freqs}{Frequencies in Hz (default: 128 points from 0 to the Nyquist
frequency).}

\item{normalized}{Row-normalize the DTF (default: \code{TRUE}).}

\item{ffDTF}{Use the full-frequency DTF normalization (default: \code{FALSE}).}

\item{band}{Optional length-2 band in Hz over which to average the returned
matrix (default: all frequencies).}

\item{mvar_method}{MVAR estimator (default: \code{"ols"}).}

\item{modality_x, modality_y, channels_x, channels_y}{Passed to the shared
signal-pair extraction when \code{y} is supplied.}
}
\value{
A list with \code{dtf} (n x n x n_freqs array), the band-averaged
directed \code{matrix} (rows = targets, columns = sources),
\code{frequencies}, \code{channels}, and \code{order}. For a two-channel
input, \code{dtf_xy} and \code{dtf_yx} give the directional summaries.
}
\description{
Frequency-resolved directed connectivity via the Directed Transfer Function
(Kaminski & Blinowska 1991), computed from the transfer function of a
multivariate autoregressive model fitted with \code{\link[PhysioCore:mvarFit]{PhysioCore::mvarFit()}}. This
cross-modal implementation is numerically identical to \code{PhysioEEG}'s
\code{eegDTF} on the same input. Because the transfer function inverts the
whole VAR system, DTF reflects both direct and indirect pathways.
}
\examples{
set.seed(1)
X <- matrix(rnorm(1500), ncol = 3)
res <- directedTransferFunction(X, sr = 100, order = 4)
dim(res$dtf)
}
\references{
Kaminski, M. J., & Blinowska, K. J. (1991). A new method of the description
of the information flow in the brain structures. Biological Cybernetics,
65(3), 203-210.
}
\seealso{
\code{\link[=partialDirectedCoherence]{partialDirectedCoherence()}}, \code{\link[=transferEntropy]{transferEntropy()}}, \code{\link[=grangerCausality]{grangerCausality()}}
}
