% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-phase.R
\name{weightedPLI}
\alias{weightedPLI}
\title{Weighted Phase Lag Index (wPLI)}
\usage{
weightedPLI(
  x,
  y = NULL,
  sr = NULL,
  freq_band,
  debiased = TRUE,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L
)
}
\arguments{
\item{x}{Numeric vector, PhysioExperiment, or MultiPhysioExperiment.}

\item{y}{Numeric vector or PhysioExperiment (NULL when \code{x} is an MPE).}

\item{sr}{Numeric sampling rate in Hz (required when x/y are numeric).}

\item{freq_band}{Numeric vector of length 2 specifying the frequency band
\code{c(low, high)} in Hz. Required.}

\item{debiased}{Logical; if TRUE (default), also compute the debiased wPLI
estimator.}

\item{modality_x}{Character modality name in MPE for x signal.}

\item{modality_y}{Character modality name in MPE for y signal.}

\item{channels_x}{Integer channel index to extract from x (default 1).}

\item{channels_y}{Integer channel index to extract from y (default 1).}
}
\value{
A named list with components:
\describe{
\item{wpli}{Numeric scalar in [0, 1] -- the weighted PLI.}
\item{wpli_debiased}{Numeric scalar -- the debiased wPLI, or NULL if
\code{debiased = FALSE}.}
\item{n_samples}{Integer -- number of time-domain samples used.}
}
}
\description{
Computes the weighted Phase Lag Index between two signals. wPLI weights
each phase-difference sample by the magnitude of the imaginary component
of the cross-spectrum, reducing the influence of noise sources that produce
phase differences near 0 or pi.
}
\details{
\deqn{\text{wPLI} = \frac{\left|\sum |\text{Im}(S_{xy})| \cdot \text{sign}(\text{Im}(S_{xy}))\right|}{\sum |\text{Im}(S_{xy})|}}{wPLI = |sum(|Im(Sxy)| * sign(Im(Sxy)))| / sum(|Im(Sxy)|)}

When \code{debiased = TRUE}, the debiased estimator from Vinck et al. (2011) is
also returned:
\deqn{\text{wPLI}_{\text{debiased}} = \frac{(\sum \text{Im}(S_{xy}))^2 - \sum \text{Im}(S_{xy})^2}{(\sum |\text{Im}(S_{xy})|)^2 - \sum \text{Im}(S_{xy})^2}}
}
\examples{
sr <- 500
t <- seq(0, 2, length.out = sr * 2)
x <- sin(2 * pi * 10 * t)
y <- sin(2 * pi * 10 * t + pi / 4)
result <- weightedPLI(x, y, sr = sr, freq_band = c(8, 12))
result$wpli
}
\references{
Vinck, M., Oostenveld, R., van Wingerden, M., Battaglia, F., &
Pennartz, C. M. A. (2011). An improved index of phase-synchronization
for electrophysiological data in the presence of volume-conduction, noise
and sample-size bias. \emph{NeuroImage}, 55(4), 1548--1565.

Lachaux, J.-P., Rodriguez, E., Martinerie, J., & Varela, F. J. (1999).
Measuring phase synchrony in brain signals. \emph{Human Brain Mapping},
8(4), 194--208.
}
\seealso{
\code{\link[=phaseLockingValue]{phaseLockingValue()}}, \code{\link[=phaseLagIndex]{phaseLagIndex()}}, \code{\link[=couplingAnalysis]{couplingAnalysis()}}
}
