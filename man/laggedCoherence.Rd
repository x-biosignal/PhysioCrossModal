% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-spectral.R
\name{laggedCoherence}
\alias{laggedCoherence}
\title{Lagged coherence between two signals}
\usage{
laggedCoherence(
  x,
  y = NULL,
  sr = NULL,
  freq_range = NULL,
  nperseg = 256L,
  noverlap = NULL,
  method = c("welch", "multitaper"),
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L
)
}
\arguments{
\item{x}{Numeric vector, PhysioExperiment, or MultiPhysioExperiment.}

\item{y}{Numeric vector or PhysioExperiment, or NULL when \code{x} is an MPE.}

\item{sr}{Numeric sampling rate in Hz (required when x/y are numeric).}

\item{freq_range}{Numeric vector of length 2 giving the frequency range
\code{c(low, high)} in Hz to restrict the output. NULL returns all
frequencies.}

\item{nperseg}{Integer segment length for Welch's method (default 256).}

\item{noverlap}{Integer overlap between segments, or NULL for
\code{floor(nperseg / 2)}.}

\item{method}{Character spectral estimation method. Currently only
\code{"welch"} is implemented; \code{"multitaper"} is reserved for
future use.}

\item{modality_x}{Character modality name in MPE for the x signal.}

\item{modality_y}{Character modality name in MPE for the y signal.}

\item{channels_x}{Integer which channel to extract from x (default 1).}

\item{channels_y}{Integer which channel to extract from y (default 1).}
}
\value{
A named list as returned by \code{\link{coherence}}, with
\code{type = "lagged"}.
}
\description{
Convenience wrapper for \code{\link{coherence}} with \code{type = "lagged"},
the lagged (non-instantaneous) coherence of Pascual-Marqui (2007),
\code{Im(C) / sqrt(1 - Re(C)^2)} where \code{C} is the complex coherency. Like
the imaginary part of coherency it is insensitive to zero-lag /
volume-conduction coupling.
}
\examples{
sr <- 200
t <- seq(0, 10, length.out = sr * 10)
x <- sin(2 * pi * 10 * t)
y <- sin(2 * pi * 10 * t + pi / 2)          # 90-degree lag
lc <- laggedCoherence(x, y, sr = sr, freq_range = c(8, 12))
max(abs(lc$coherence))
}
\references{
Pascual-Marqui, R. D. (2007). Instantaneous and lagged measurements of linear
and nonlinear dependence between groups of multivariate time series.
\emph{arXiv:0711.1455}.
}
\seealso{
\code{\link{coherence}}
}
