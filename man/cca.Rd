% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-twoblock.R
\name{cca}
\alias{cca}
\title{Canonical correlation analysis between two modalities}
\usage{
cca(X, Y, ncomp = NULL, lambda_x = 0, lambda_y = 0)
}
\arguments{
\item{X, Y}{Matrices with the same \code{n} rows (observations); columns are the
features of each modality.}

\item{ncomp}{Number of canonical pairs (default: \code{min(ncol(X), ncol(Y))}).}

\item{lambda_x, lambda_y}{Ridge penalties; if \code{0} (default) a small ridge is
added automatically when a block is rank-deficient (features >= n).}
}
\value{
a \code{cca} object: \code{cor} (canonical correlations, decreasing), \code{xcoef},
\code{ycoef} (canonical vectors), \code{xscores}, \code{yscores} (canonical variates).
}
\description{
Finds the linear combinations of two modality blocks whose scores are maximally
correlated -- the directions in which the modalities co-vary. Ridge
regularisation keeps it well-posed when a block has more features than
observations.
}
\examples{
set.seed(1); z <- matrix(rnorm(50 * 2), 50, 2)
X <- z \%*\% matrix(rnorm(2 * 5), 2, 5) + matrix(rnorm(50 * 5, 0, .3), 50, 5)
Y <- z \%*\% matrix(rnorm(2 * 4), 2, 4) + matrix(rnorm(50 * 4, 0, .3), 50, 4)
cca(X, Y)$cor
}
\references{
Hotelling H (1936); Vinod HD (1976) ridge CCA.
}
\seealso{
\code{\link[=plsBlocks]{plsBlocks()}}, \code{\link[=rvCoefficient]{rvCoefficient()}}
}
