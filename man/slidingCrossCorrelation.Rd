% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-timedom.R
\name{slidingCrossCorrelation}
\alias{slidingCrossCorrelation}
\title{Sliding-window cross-correlation}
\usage{
slidingCrossCorrelation(
  x,
  y = NULL,
  sr = NULL,
  window_sec = 1,
  step_sec = 0.5,
  max_lag = NULL,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L
)
}
\arguments{
\item{x}{Numeric vector, PhysioExperiment, or MultiPhysioExperiment.}

\item{y}{Numeric vector or PhysioExperiment, or NULL when \code{x} is
a MultiPhysioExperiment.}

\item{sr}{Numeric sampling rate in Hz (required when x/y are numeric).}

\item{window_sec}{Numeric window length in seconds (default 1).}

\item{step_sec}{Numeric step size in seconds (default 0.5).}

\item{max_lag}{Integer maximum lag in samples for each window. If NULL,
defaults to \code{floor(window_samples / 4)}.}

\item{modality_x}{Character modality name in MPE for the x signal.}

\item{modality_y}{Character modality name in MPE for the y signal.}

\item{channels_x}{Integer which channel to extract from x (default 1).}

\item{channels_y}{Integer which channel to extract from y (default 1).}
}
\value{
A named list with components:
\describe{
\item{correlations}{Numeric matrix of dimensions (n_windows x n_lags)
containing cross-correlation values.}
\item{times}{Numeric vector of window centre times in seconds.}
\item{lags}{Integer vector of lag values in samples.}
\item{peak_lags}{Numeric vector of peak lags (in samples) for each
window.}
\item{peak_correlations}{Numeric vector of peak correlation values
for each window.}
}
}
\description{
Computes cross-correlation in sliding (overlapping) windows to track
how time-domain coupling varies over time. For each window position,
\code{\link{crossCorrelation}} is called and the results are assembled
into a matrix.
}
\examples{
sr <- 500
set.seed(1)
x <- rnorm(5000)
y <- c(rep(0, 10), x[1:4990])
result <- slidingCrossCorrelation(x, y, sr = sr,
                                   window_sec = 1, step_sec = 0.5)
dim(result$correlations)
result$peak_lags
}
\references{
Chatfield, C. (2004). \emph{The Analysis of Time Series: An
Introduction} (6th ed.). Chapman & Hall/CRC.
}
\seealso{
\code{\link[=crossCorrelation]{crossCorrelation()}}, \code{\link[=plotCouplingTimecourse]{plotCouplingTimecourse()}},
\code{\link[=couplingAnalysis]{couplingAnalysis()}}
}
