% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align-elastic.R
\name{srvfMean}
\alias{srvfMean}
\title{Elastic (Karcher) mean of a set of curves}
\usage{
srvfMean(curves, t = NULL, iterations = 5L, max_step = 6L, use_fdasrvf = NA)
}
\arguments{
\item{curves}{A list of numeric curves of the same length, or a matrix whose
columns are curves.}

\item{t}{Optional grid (defaults to 0..1).}

\item{iterations}{Number of refinement iterations (default: 5).}

\item{max_step}{Maximum DP step for the pure-R solver (default: 6).}

\item{use_fdasrvf}{As in \code{\link[=elasticAlign]{elasticAlign()}}.}
}
\value{
A list with the elastic \code{mean} curve, the \code{warpings} (a
matrix, one column per curve), and the \code{aligned} curves.
}
\description{
Computes the elastic template mean of repeated curves by iteratively aligning
every curve to the current mean and averaging the aligned curves. Because the
phase variability is removed before averaging, peaks are preserved rather than
blurred out (unlike a naive cross-curve mean).
}
\examples{
t <- seq(0, 1, length.out = 80)
curves <- lapply(c(-0.1, 0, 0.1), function(s)
  exp(-((t - (0.5 + s))^2) / (2 * 0.05^2)))
m <- srvfMean(curves)
max(m$mean)
}
\seealso{
\code{\link[=elasticAlign]{elasticAlign()}}
}
