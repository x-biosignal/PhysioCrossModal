% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-frontier.R
\name{leidaStates}
\alias{leidaStates}
\title{LEiDA dynamic functional-connectivity states}
\usage{
leidaStates(
  x,
  sr,
  freq_band,
  n_states = 4L,
  order = 4L,
  nstart = 10L,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix, time (rows) by channels (columns).}

\item{sr}{Sampling rate in Hz.}

\item{freq_band}{Numeric \code{c(low, high)} band in Hz.}

\item{n_states}{Number of clusters/states (default 4).}

\item{order}{Butterworth filter order (default 4).}

\item{nstart}{k-means restarts (default 10).}

\item{seed}{Optional integer seed for reproducible clustering.}
}
\value{
An object of class \code{"leida"}: a list with \code{states} (per-time cluster
labels), \code{centroids} (states x channels leading-eigenvector centroids),
\code{occupancy} (fraction of time in each state), \code{dwell} (mean run length in
samples per state), and \code{n_states}.
}
\description{
Leading Eigenvector Dynamics Analysis (Cabral et al. 2017): at each time
point the instantaneous phase-coherence matrix is reduced to its leading
eigenvector; these are clustered over time into a small set of recurrent
connectivity states. Complements state-resolved network analysis by finding
the states data-drivenly rather than from external labels.
}
\examples{
set.seed(1); sr <- 100; n <- 2000; t <- seq_len(n) / sr
# two regimes: channels 1-2 coherent first half, 3-4 coherent second half
base <- sin(2 * pi * 10 * t)
X <- cbind(base + rnorm(n, sd = .3), base + rnorm(n, sd = .3),
           sin(2 * pi * 10 * t + 1) + rnorm(n, sd = .3),
           sin(2 * pi * 10 * t + 1) + rnorm(n, sd = .3))
res <- leidaStates(X, sr = sr, freq_band = c(8, 12), n_states = 2, seed = 1)
res$occupancy
}
\references{
Cabral J, et al. (2017). Cognitive performance in healthy older
adults relates to spontaneous switching between states of functional
connectivity during rest. \emph{Sci Rep} 7:5135.
}
