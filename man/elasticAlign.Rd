% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align-elastic.R
\name{elasticAlign}
\alias{elasticAlign}
\title{Elastic (SRVF) alignment of two curves}
\usage{
elasticAlign(
  x,
  y,
  t = NULL,
  lambda = 0,
  method = c("srvf"),
  max_step = 6L,
  smooth = FALSE,
  use_fdasrvf = NA
)
}
\arguments{
\item{x, y}{Numeric curves of the same length.}

\item{t}{Optional grid (defaults to an evenly spaced grid on 0..1).}

\item{lambda}{Non-negative warping-roughness penalty (passed to \pkg{fdasrvf}
when used; the pure-R solver ignores it beyond boundary/monotonicity).}

\item{method}{Currently \code{"srvf"}.}

\item{max_step}{Maximum DP step (bounds the local warping slope) for the
pure-R solver (default: 6).}

\item{smooth}{Smooth the pure-R warping with a monotone spline (default:
\code{FALSE}).}

\item{use_fdasrvf}{\code{NA} (default) uses \pkg{fdasrvf} if installed;
\code{TRUE} requires it; \code{FALSE} forces the pure-R solver.}
}
\value{
A list with \code{gamma} (the warping), \code{aligned} (\code{y}
warped to \code{x}), \code{amplitude_distance}, \code{phase_distance}, the
SRVFs, and the solver used.
}
\description{
Aligns curve \code{y} to curve \code{x} by an optimal time warping computed in
the Square-Root Velocity Function representation, separating phase (the
warping) from amplitude (the residual shape difference). A pure-R
dynamic-programming solver is used by default; if the \pkg{fdasrvf} package is
available it is used for higher accuracy.
}
\examples{
t <- seq(0, 1, length.out = 80)
x <- exp(-((t - 0.5)^2) / (2 * 0.08^2))
y <- exp(-((t - 0.65)^2) / (2 * 0.08^2))
al <- elasticAlign(x, y)
al$amplitude_distance
}
\references{
Srivastava, A., Wu, W., Kurtek, S., Klassen, E., & Marron, J. S. (2011).
Registration of functional data using Fisher-Rao metric. arXiv:1103.3817.
}
\seealso{
\code{\link[=srvfMean]{srvfMean()}}, \code{\link[=warpApply]{warpApply()}}
}
