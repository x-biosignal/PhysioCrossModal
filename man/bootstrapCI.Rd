% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-significance.R
\name{bootstrapCI}
\alias{bootstrapCI}
\title{Bootstrap confidence interval for coupling}
\usage{
bootstrapCI(
  x,
  y = NULL,
  sr = NULL,
  method,
  n_boot = 199L,
  ci = 0.95,
  block_len = NULL,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L,
  cores = 1L,
  ...
)
}
\arguments{
\item{x}{Numeric vector, PhysioExperiment, or MultiPhysioExperiment.}

\item{y}{Numeric vector or PhysioExperiment, or NULL when \code{x} is an
MPE.}

\item{sr}{Numeric sampling rate in Hz (required when x/y are numeric).}

\item{method}{Character coupling method (same options as
\code{\link{surrogateTest}}).}

\item{n_boot}{Integer number of bootstrap replicates (default 199).}

\item{ci}{Numeric confidence level (default 0.95).}

\item{block_len}{Integer block length for block bootstrap, or NULL for
automatic (\code{ceiling(sqrt(n))}).}

\item{modality_x, modality_y}{Character modality names for MPE input.}

\item{channels_x, channels_y}{Integer channel indices (default 1).}

\item{cores}{Integer number of parallel cores to use (default \code{1L}).
When \code{cores > 1}, bootstrap replicates are computed in parallel.}

\item{...}{Additional arguments passed to the coupling function.}
}
\value{
A list with components:
\describe{
\item{observed}{The full coupling result from the original signals.}
\item{statistic}{Numeric scalar: the extracted coupling statistic.}
\item{ci_lower}{Numeric scalar: lower CI bound.}
\item{ci_upper}{Numeric scalar: upper CI bound.}
\item{ci_level}{Numeric scalar: confidence level used.}
\item{boot_distribution}{Numeric vector of bootstrap statistics.}
}
}
\description{
Computes a bootstrap confidence interval for the coupling statistic
between two signals using the moving-block bootstrap (to preserve
temporal autocorrelation).
}
\examples{
sr <- 500
t <- seq(0, 2, length.out = sr * 2)
x <- sin(2 * pi * 10 * t) + 0.3 * rnorm(length(t))
y <- 0.8 * sin(2 * pi * 10 * t) + 0.3 * rnorm(length(t))
result <- bootstrapCI(x, y, sr = sr, method = "coherence",
                      n_boot = 19, nperseg = 128L)
c(result$ci_lower, result$ci_upper)
}
\references{
Efron, B., & Tibshirani, R. J. (1993). \emph{An Introduction to the
Bootstrap}. Chapman & Hall/CRC.
}
\seealso{
\code{\link[=surrogateTest]{surrogateTest()}}, \code{\link[=couplingAnalysis]{couplingAnalysis()}}
}
