% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-index.R
\name{multimodalGaitIndex}
\alias{multimodalGaitIndex}
\title{Composite multi-modal gait deviation index}
\usage{
multimodalGaitIndex(
  features,
  reference,
  blocks = NULL,
  ve = 0.95,
  mean_index = 100,
  sd_index = 10
)
}
\arguments{
\item{features}{An \verb{n x p} matrix of subjects' multi-modal gait features
(kinematics, kinetics, EMG summaries, spatiotemporal, ... concatenated).}

\item{reference}{An \verb{m x p} matrix of the same features for a normative
(control) sample.}

\item{blocks}{Optional named list mapping each modality to its column indices
in \code{features}, for a per-modality breakdown.}

\item{ve}{Reference variance retained by the PCA whitening (default 0.95).}

\item{mean_index, sd_index}{Scaling (default 100 and 10, as for GDI).}
}
\value{
a \code{multimodal_index} object: \code{index} (per subject; ~100 = typical),
\code{distance}, \code{reference_mean_index}, and (if \code{blocks} given) \code{by_modality}
(subject x modality index matrix).
}
\description{
A Gait-Deviation-Index-style score (Schwartz & Rozumalski 2008) generalised
across modalities: how far each subject's multi-modal gait feature vector lies
from a normative reference cloud, scaled so a typical control scores ~100 and
every 10 points is one standard deviation of deviation. Optionally broken down
per modality to show which modality drives the deviation.
}
\examples{
set.seed(1); ctrl <- matrix(rnorm(60 * 8), 60, 8)
subj <- rbind(matrix(rnorm(5 * 8), 5, 8),               # typical
              matrix(rnorm(5 * 8, 3), 5, 8))            # deviant
multimodalGaitIndex(subj, ctrl)$index
}
\references{
Schwartz MH, Rozumalski A (2008) Gait Posture 28:351-357.
}
\seealso{
\code{\link[=jive]{jive()}}, \code{\link[=fuseBlocks]{fuseBlocks()}}
}
