% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-wrapper.R
\name{couplingAnalysisCached}
\alias{couplingAnalysisCached}
\title{Cached coupling analysis over a MultiPhysioExperiment}
\usage{
couplingAnalysisCached(
  mpe,
  method,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L,
  ...,
  cache = TRUE
)
}
\arguments{
\item{mpe}{A \code{MultiPhysioExperiment} object.}

\item{method}{Character coupling method (see \code{\link{couplingAnalysis}}).}

\item{modality_x, modality_y}{Character modality names to extract from \code{mpe}.}

\item{channels_x, channels_y}{Integer channel indices (default 1).}

\item{...}{Additional arguments forwarded to \code{couplingAnalysis()} and
folded into the cache key.}

\item{cache}{Logical; if \code{TRUE} (default) reuse and populate the cache.
\code{FALSE} forces recomputation but still stores the fresh result.}
}
\value{
A list with \code{result} (the coupling result), \code{mpe} (the
object with its \code{couplingResults} cache updated) and \code{cached}
(\code{TRUE} when the result was served from the cache).
}
\description{
Wraps \code{\link{couplingAnalysis}()} with a per-object memoisation cache
held in the \code{couplingResults} slot of a
\code{\link{MultiPhysioExperiment}}. Results are keyed by a
\code{\link[digest]{digest}} of the analysis arguments (method, modalities,
channels, and any extra arguments), so a repeated identical request returns
the stored result without recomputing.
}
\examples{
eeg <- PhysioExperiment(assays = list(raw = matrix(rnorm(1000), 500, 2)),
                        samplingRate = 250)
emg <- PhysioExperiment(assays = list(raw = matrix(rnorm(1000), 500, 2)),
                        samplingRate = 250)
mpe <- MultiPhysioExperiment(EEG = eeg, EMG = emg)
out  <- couplingAnalysisCached(mpe, "coherence", "EEG", "EMG")
out2 <- couplingAnalysisCached(out$mpe, "coherence", "EEG", "EMG")
out2$cached  # TRUE
}
\seealso{
\code{\link[=couplingAnalysis]{couplingAnalysis()}}, \code{\link[=couplingResults]{couplingResults()}}
}
