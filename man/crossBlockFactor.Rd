% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-index.R
\name{crossBlockFactor}
\alias{crossBlockFactor}
\title{Cross-block common-factor model}
\usage{
crossBlockFactor(blocks, nfactors, load_threshold = 0.4)
}
\arguments{
\item{blocks}{A list of >= 2 matrices, each \verb{n x p_k} (shared rows). Requires
more observations than total features.}

\item{nfactors}{Number of common factors.}

\item{load_threshold}{A factor is considered to load on a block if any of the
block's variables has \verb{|loading| >=} this (default 0.4).}
}
\value{
a \code{crossblock_factor} object: \code{loadings} (variables x factors, with a
\code{block} attribute), \code{shared} (logical per factor: loads on >= 2 blocks),
\code{n_shared}, and the fitted \code{factanal}.
}
\description{
Fits a maximum-likelihood common-factor model to the pooled, standardised
modality blocks and flags which latent factors are SHARED across modalities
(load on more than one block) versus modality-specific -- a latent-variable
view of multi-modal integration.
}
\examples{
set.seed(1); f <- matrix(rnorm(80 * 1), 80, 1)
a <- f \%*\% matrix(rnorm(4), 1, 4) + matrix(rnorm(80 * 4, 0, .4), 80, 4)
b <- f \%*\% matrix(rnorm(3), 1, 3) + matrix(rnorm(80 * 3, 0, .4), 80, 3)
crossBlockFactor(list(a = a, b = b), nfactors = 1)$shared
}
\references{
Spearman/Thurstone common-factor model; McDonald (1985).
}
\seealso{
\code{\link[=jive]{jive()}}, \code{\link[=multipleFactorAnalysis]{multipleFactorAnalysis()}}
}
