% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align-signals.R
\name{mergePhysio}
\alias{mergePhysio}
\title{Merge two PhysioExperiment objects by combining channels}
\usage{
mergePhysio(x, y, prefix = c("x_", "y_"))
}
\arguments{
\item{x}{A \code{PhysioExperiment} object.}

\item{y}{A \code{PhysioExperiment} object.}

\item{prefix}{Character vector of length 2: prefixes added to channel
labels from \code{x} and \code{y} respectively (default
\code{c("x_", "y_")}).}
}
\value{
A single \code{PhysioExperiment} with the columns of both inputs.
}
\description{
Horizontally concatenates the channels (columns) of two
\code{PhysioExperiment} objects that share the \strong{same} sampling rate.
Channel labels are prefixed to avoid name collisions.
}
\details{
Both objects must have the same number of time points (rows) and the same
sampling rate. Only the first assay of each object is merged.
}
\examples{
pe1 <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(100 * 4), nrow = 100)),
  colData = S4Vectors::DataFrame(label = paste0("A", 1:4)),
  samplingRate = 500
)
pe2 <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(100 * 4), nrow = 100)),
  colData = S4Vectors::DataFrame(label = paste0("B", 1:4)),
  samplingRate = 500
)
merged <- mergePhysio(pe1, pe2)
}
\seealso{
\code{\link[=alignToRate]{alignToRate()}}, \code{\link[=alignSignals]{alignSignals()}},
\code{\link[=MultiPhysioExperiment]{MultiPhysioExperiment()}}
}
