% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-spectral.R
\name{multitaperCoherence}
\alias{multitaperCoherence}
\title{Multitaper coherence}
\usage{
multitaperCoherence(
  x,
  y = NULL,
  sr = NULL,
  nw = 4,
  k = NULL,
  freq_range = NULL,
  modality_x = NULL,
  modality_y = NULL,
  channels_x = 1L,
  channels_y = 1L,
  ...
)
}
\arguments{
\item{x}{Numeric vector, PhysioExperiment, or MultiPhysioExperiment.}

\item{y}{Numeric vector or PhysioExperiment, or NULL when \code{x} is an
MPE.}

\item{sr}{Numeric sampling rate in Hz (required when x/y are numeric).}

\item{nw}{Numeric time-bandwidth product (default 4). Controls the
trade-off between frequency resolution and variance.}

\item{k}{Integer number of tapers (default \code{2 * nw - 1}).}

\item{freq_range}{Optional numeric vector \code{c(low, high)} to restrict
output frequencies.}

\item{modality_x, modality_y}{Character modality names for MPE input.}

\item{channels_x, channels_y}{Integer channel indices (default 1).}

\item{...}{Currently unused.}
}
\value{
A list with components:
\describe{
\item{coherence}{Numeric vector of magnitude-squared coherence values
in \eqn{[0, 1]}.}
\item{frequencies}{Numeric vector of corresponding frequencies in Hz.}
\item{confidence_limit}{Numeric scalar giving the 95\\% confidence
threshold based on the number of tapers.}
}
}
\description{
Computes magnitude-squared coherence using multitaper spectral estimation
with Discrete Prolate Spheroidal Sequences (DPSS / Slepian tapers).
Multitaper estimation provides lower variance than Welch's method for
short signals or when frequency resolution is critical.
}
\examples{
sr <- 500
t <- seq(0, 10, length.out = sr * 10)
x <- sin(2 * pi * 20 * t) + 0.2 * rnorm(length(t))
y <- 0.8 * sin(2 * pi * 20 * t) + 0.2 * rnorm(length(t))
result <- multitaperCoherence(x, y, sr = sr)
}
\references{
Thomson, D. J. (1982). Spectrum estimation and harmonic analysis.
\emph{Proceedings of the IEEE}, 70(9), 1055--1096.

Carter, G. C. (1987). Coherence and time delay estimation.
\emph{Proceedings of the IEEE}, 75(2), 236--255.
}
\seealso{
\code{\link[=coherence]{coherence()}}, \code{\link[=crossSpectrum]{crossSpectrum()}}, \code{\link[=couplingAnalysis]{couplingAnalysis()}}
}
