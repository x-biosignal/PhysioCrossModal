% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion-coupled.R
\name{coupledNMF}
\alias{coupledNMF}
\title{Coupled non-negative matrix factorization across two modalities}
\usage{
coupledNMF(
  X,
  Y,
  rank,
  max_iter = 500L,
  tol = 1e-07,
  n_restart = 5L,
  seed = NULL
)
}
\arguments{
\item{X, Y}{Non-negative matrices with the same \code{n} rows (shared observations,
e.g. time or gait-cycle points); columns are each modality's channels.}

\item{rank}{Number of shared factors.}

\item{max_iter, tol}{Iteration budget and convergence tolerance.}

\item{n_restart}{Random restarts (best kept; default 5).}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{coupled_nmf} object: \code{shared} (\verb{n x rank} shared factor \code{W}), \code{Hx},
\code{Hy} (\verb{rank x p} and \verb{rank x q} loadings), \code{vaf_x}, \code{vaf_y}, \code{vaf} (combined),
\code{iterations}.
}
\description{
Factorises two non-negative blocks that share the observation dimension into a
SHARED factor matrix \code{W} and modality-specific loadings \code{Hx}, \code{Hy}, minimising
\verb{||X - W Hx||^2 + ||Y - W Hy||^2}. The shared \code{W} is the common latent drive
recovered jointly from both modalities.
}
\examples{
set.seed(1); W <- matrix(runif(50 * 2), 50, 2)      # shared drive
X <- W \%*\% matrix(runif(2 * 6), 2, 6); Y <- W \%*\% matrix(runif(2 * 4), 2, 4)
fit <- coupledNMF(X, Y, rank = 2, n_restart = 2)
fit$vaf
}
\references{
Lee & Seung (2001) NMF; Cichocki et al. (2009) coupled/joint NMF.
}
\seealso{
\code{\link[=plsBlocks]{plsBlocks()}}, \code{\link[=jive]{jive()}}
}
